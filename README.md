# javascript

Test micro service which reads data stored in json format from a kafka queue, 
processes the data, converts it back to json format and pushes it to a kafka queue. 

 git clone -b development     https://github.com/floodta/javascript.git
 cd javascript/
 git checkout development

